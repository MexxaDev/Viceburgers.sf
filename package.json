{
  "name": "syntra-pos",
  "version": "1.0.0",
  "private": true,
  "description": "POS Premium - Sistema de Gestión",
  "scripts": {
    "lint": "eslint --ext .js .",
    "format": "prettier --write \"**/*.{js,css,html,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,css,html,json,md}\""
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "prettier": "^3.2.0"
  }
}
